name: Release Build

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  release-build:
    name: Build release artifacts
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event.release.tag_name }}
      # TODO build/sign/attach steps go here next
